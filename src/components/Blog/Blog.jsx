import { Helmet } from "react-helmet-async";

const Blog = () => {
    return (
        <div>
            <Helmet>
                <title>career hub || blog</title>
            </Helmet>
            <h1>BLog</h1>
        </div>
    );
};

export default Blog;