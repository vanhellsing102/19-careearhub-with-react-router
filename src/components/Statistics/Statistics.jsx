import { Helmet } from "react-helmet-async";

const Statistics = () => {
    return (
        <div>
            <Helmet>
                <title>career hub || statistics</title>
            </Helmet>
            <h1>Statistics</h1>
        </div>
    );
};

export default Statistics;